use temperature_conv::*;

fn main() {
    println!(
        "137 Fahrenheit to Celsius: {}",
        fahrenheit_to_celsius(137 as f64)
    );

    println!(
        "0 Celsius to Fahrenheit : {}",
        celsius_to_fahrenheit(0 as f64)
    );
}
